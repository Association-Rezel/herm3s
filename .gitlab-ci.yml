stages:
  - linting
  - build-tests

linting:
  stage: linting
  image: python
  script:
    - pip install black
    - black --check .

build-tests:
  stage: testing
  image: docker
  script:
    - docker build -t rezel/hermes .
