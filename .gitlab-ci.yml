stages:
  - linting
  - build-tests
  - unit-tests

#test

#run unit-tests which launch command docker compose up --build with the docker compose in my project
unit-tests:
  stage: unit-tests
  image: docker:24.0.7
  services:
    - docker:24.0.7-dind
  script:
    - ls
    - docker --help
    - docker --version
  



# linting:
#   stage: linting
#   image: python
#   script:
#     - pip install black
#     - black --check .