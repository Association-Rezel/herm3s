stages:
  - linting
  - build-tests
  - unit-tests

#test

#run unit-tests which launch command docker compose up --build
unit-tests:
  stage: unit-tests
  image: docker:19.03.12
  services:
    - docker:19.03.12-dind
  script:
    - docker-compose up --build -d
  



# linting:
#   stage: linting
#   image: python
#   script:
#     - pip install black
#     - black --check .