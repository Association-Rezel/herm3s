stages:
  - linting
  - build-tests
  - unit-tests


unit-tests:
  stage: unit-tests
  image: docker:24.0.7
  services:
    - docker:24.0.7-dind
  
  script:
    - docker compose -f docker-compose.unit_tests.yaml up --build --exit-code-from unit_tests  


  

# linting:
#   stage: linting
#   image: python
#   script:
#     - pip install black
#     - black --check .